<h1><%= @post.title %></h1>

<% if user_signed_in? %>
<%= link_to 'Изменить', edit_post_path(@post) %> 
|
<%= link_to "Удалить", post_path(@post), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %>
<% end %>

<p><%= @post.body %></p>